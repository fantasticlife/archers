<div class="section">
	<%= content_tag( 'h1', "Add universe details" ) %>
</div>

<%= form_for @universe, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add details', :class => 'submit' ) %>
<% end %>