<div class="section">
	<%= content_tag( 'h1', "Add a new programme" ) %>
</div>

<%= form_for @tleo, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add programme', :class => 'submit' ) %>
<% end %>