<div class="section">
	<%= content_tag( 'h1', "#{@character.name} played by #{@casting.actor.name} #{content_tag( 'span', link_to( 'Edit', casting_edit_url))}".html_safe ) %>
</div>

<div class="section">
	<%= render :partial => 'casting_details', :object => @casting %>
</div>