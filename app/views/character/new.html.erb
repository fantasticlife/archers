<div class="section">
	<%= content_tag( 'h1', "Add a new character" ) %>
</div>

<%= form_for @character, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add character', :class => 'submit' ) %>
<% end %>