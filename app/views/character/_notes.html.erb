<div class="section">
	<%= content_tag( 'h2', 'Character notes:' ) %>
	<% if notes.empty? -%>
		<%= content_tag( 'p', 'No notes to show.' ) %>
	<% else -%>
		<ol>
			<%= render :partial => 'character_note/character_note', :collection => notes %>
		</ol>
	<% end -%>
	<%= content_tag( 'h3', 'Add new' ) %>
	<%= content_tag( 'p', link_to( 'Add a new note', character_note_new_url ) ) %>
</div>