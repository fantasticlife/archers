<div class="section">
	<%= content_tag( 'h1', "#{@character.name} - Add belonging" ) %>
</div>

<%= form_for @belonging, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add belonging', :class => 'submit' ) %>
<% end %>