<div class="section">
	<%= content_tag( 'h1', "#{link_to( @group.label, group_show_url( :group => @group.guid ) )} Edit".html_safe ) %>
</div>

<%= form_for @group, :url => { :action => 'update', :group_to_update => @group.guid } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Update', :class => 'submit' ) %>
<% end %>