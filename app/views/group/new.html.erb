<div class="section">
	<%= content_tag( 'h1', "Add a new group" ) %>
</div>

<%= form_for @group, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add group', :class => 'submit' ) %>
<% end %>