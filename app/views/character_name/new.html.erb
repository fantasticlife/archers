<div class="section">
	<%= content_tag( 'h1', "#{@character.name} - Add a new name" ) %>
</div>

<%= form_for @name, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add name', :class => 'submit' ) %>
<% end %>