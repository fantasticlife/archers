<div class="section">
	<%= content_tag( 'h1', "#{link_to( @character.name, character_show_url)} - Residence at #{@residence.place.title} #{content_tag( 'span', link_to( 'Edit', residence_edit_url))}".html_safe ) %>
</div>
<%= render :partial => 'residence_details', :object => @residence %>