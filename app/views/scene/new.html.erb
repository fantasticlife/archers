<div class="section">
	<%= content_tag( 'h1', "Add a new scene" ) %>
</div>

<%= form_for @scene, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add scene', :class => 'submit' ) %>
<% end %>