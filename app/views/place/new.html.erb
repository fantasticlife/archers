<div class="section">
	<%= content_tag( 'h1', "Add a new place" ) %>
</div>

<%= form_for @place, :url => { :action => 'create' } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Add place', :class => 'submit' ) %>
<% end %>