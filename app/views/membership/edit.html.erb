<div class="section">
	<%= content_tag( 'h1', "#{link_to( @character.name, character_show_url( :character => @character ) )} - Edit membership".html_safe ) %>
</div>

<%= form_for @membership, :url => { :action => 'update', :membership_to_update => @membership } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Update', :class => 'submit' ) %>
<% end %>