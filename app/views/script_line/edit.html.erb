<div class="section">
	<%= content_tag( 'h1', "#{link_to( @scene.display_title_with_episode, scene_show_url )} - Edit line".html_safe ) %>
</div>

<%= form_for @script_line, :url => { :action => 'update', :line_to_update => @script_line.guid } do |f| %>
	<%= render :partial => 'form', :object => f %>
  	<%= submit_tag( 'Update', :class => 'submit' ) %>
<% end %>